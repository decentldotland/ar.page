{
    "version": 2,
    "name": "ar-page",
    "build": {
        "command": "next build",
        "env": {
          "NODE_ENV": "production"
        }
    },
    "routes": [
        {
        "src": "api/(.+)",
        "timeout": 30
        }
    ]
}