// @flow 
import * as React from 'react';
type Props = {
    children?: any;
    className: string;
};
export const Nav = (props: Props) => {
    return (
        <div className={props.className}>
            test
        </div>
    );
};