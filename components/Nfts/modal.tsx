// @flow 
import * as React from 'react';
// import { createPortal } from 'react-dom';

type Props = {
    isOpen: boolean;
};
export const Modal = (props: Props) => {

    return (
        <div>
        </div>
    );
};